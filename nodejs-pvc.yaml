apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodejs-claim0
spec:
  volumeName: nodejs-code-pv   #👈explicitly bind to the static PV
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi



